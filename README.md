# Personal Mechanic (Streamlit Prototype)

A mobile-first prototype that helps drivers interpret warning lights and symptoms and produces:
- urgency: Drive OK / Service Soon / Stop Now (rule-based triage)
- AI explanation + mechanic report (optional, via OpenAI API)

## Run
```bash
pip install -r requirements.txt
streamlit run app.py